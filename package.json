{
  "name": "nickis_bot",
  "version": "1.0.0",
  "description": "",
  "main": "build/bin/bot.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "node build/bin/bot.js",
    "test": "mocha test/",
    "build": "tsc",
    "build-watch": "tsc --watch",
    "typescript-install": "sudo npm install -g typescript ts-node typings && typings install env~node --save --global"
  },
  "author": "bananaCypher",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^2.5.3"
  },
  "dependencies": {
    "google-spreadsheet": "^2.0.3",
    "moment": "^2.13.0",
    "slackbots": "^0.5.1"
  }
}
